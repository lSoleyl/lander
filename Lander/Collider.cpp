#include "stdafx.h"

namespace Lander {

bool Collider::IsPointInside(Vector worldPoint) const {
  //TODO check whether the point is inside the rectangle of this object (pos,size)
  //     this check can be skipped if the point is outside this object's radius.
  throw std::exception("This hasn't been implemented yet!");
}


}
